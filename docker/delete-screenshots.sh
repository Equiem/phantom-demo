#!/usr/bin/env bash

HOST_SCREENSHOTS_DIR=`pwd`/docker/screenshots/;
HOST_RESULTS_DIR=`pwd`/docker/results/;
HOST_FAILURES_DIR=`pwd`/docker/failures/;

rm -r $HOST_SCREENSHOTS_DIR
rm -r $HOST_RESULTS_DIR
rm -r $HOST_FAILURES_DIR
